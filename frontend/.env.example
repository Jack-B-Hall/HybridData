# Serve the UI entirely from committed fixtures — no backend required.
# VITE_USE_MOCKS=1

# Override the dev-server proxy target if 127.0.0.1:8000 is unavailable
# (the default `hde serve` port, see docs/api.md).
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000
